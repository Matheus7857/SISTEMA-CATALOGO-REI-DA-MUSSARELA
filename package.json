{
  "name": "catalogo",
  "version": "1.0.0",
  "description": "Sistema Comercial de Catálogo ERP - Rei da Mussarela",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":       "node build-www.js",
    "cap:android": "npx cap add android",
    "cap:sync":    "npm run build && npx cap sync android",
    "cap:open":    "npx cap open android",
    "apk":         "npm run build && npx cap sync android && npx cap open android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@supabase/supabase-js": "^2.106.0",
    "dotenv": "^17.4.2",
    "firebase": "^12.12.1"
  }
}
